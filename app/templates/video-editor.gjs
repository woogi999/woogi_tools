import VideoEditorPage from '../components/video-editor-page';

// The editor takes the whole window, so it is not wrapped in a ToolSlot: that
// slot exists to float a tool out into picture-in-picture, and there is no page
// around this one to float it out of.
<template><VideoEditorPage /></template>
